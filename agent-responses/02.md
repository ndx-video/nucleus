Phase 1.1 done

Leak (from live test 2026-07-27)
Loader accepted any JSON with "capturedBy": "nucleus_attest". Marker-only forgeries worked at load time.

Fix

┌────────┬───────────────────────────────────────────────────────┐
│ Piece  │ Behavior                                              │
├────────┼───────────────────────────────────────────────────────┤
│ Secret │ .nucleus/attest.key — 32 random bytes, created on     │
│        │ first write, mode 0600                                │
├────────┼───────────────────────────────────────────────────────┤
│ MAC    │ integrity: "hmac-sha256:<hex>" over id, timestamp,    │
│        │ cwd, command, label, exitCode, stdout, stderr,        │
│        │ durationMs, git, fileHashes, capturedBy, version,     │
│        │ changeId                                              │
├────────┼───────────────────────────────────────────────────────┤
│ Write  │ Sign only after all field mutations (including        │
│        │ require_real_stdout notes); version: 2                │
├────────┼───────────────────────────────────────────────────────┤
│ Load   │ capturedBy filter → secret present → HMAC verify (    │
│        │ timingSafeEqual) → else null                          │
├────────┼───────────────────────────────────────────────────────┤
│ List   │ Only integrity-valid artifacts count                  │
└────────┴───────────────────────────────────────────────────────┘

Tests (22/22 pass)
• Real harness artifact loads with integrity tag  
• Marker-only forgery → rejected  
• Marker + fake MAC → rejected  
• Tampered stdout reusing real MAC → rejected  

Docs
AGENTS.md, README.md, ROADMAP.md (Phase 1 live-validation + Phase 1.1), reviewer prompt updated.

Residual trust (documented)
Stops casual model forgery. Does not stop a process that can read .nucleus/attest.key and write a matching MAC. Local-first by design — not remote attestation / external keys.