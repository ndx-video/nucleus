Phase 1.2 complete

Before (leaks)
┌─────────────────┬───────────────────────────────────┐
│ Surface         │ Old behavior                      │
├─────────────────┼───────────────────────────────────┤
│ Status / footer │ state.attestationIds.length (raw) │
├─────────────────┼───────────────────────────────────┤
│ /review gate    │ Non-empty raw ID list             │
├─────────────────┼───────────────────────────────────┤
│ Review bundle   │ Listed IDs; no re-execution       │
└─────────────────┴───────────────────────────────────┘

A. Residual polish
• Verified-only helpers: listAttestations, getVerifiedAttestations, countVerifiedAttestations, hasVerifiedAttestation, latestVerifiedAttestationId
• Status / footer: count + latest ID from integrity-verified loads only; shows gap when raw > verified
• /review gate: requires ≥1 verified artifact; if only forged/invalid IDs: clear block message

B. Independent verification
• /review default: re-runs each verified attestation command and embeds section 4. Independent re-execution (MATCH / EXIT MISMATCH / OUTPUT MISMATCH)
• Tool nucleus_verify: optional second pass (defaults to latest verified id)
• Reviewer tools: nucleus_verify injected into allow-list; write/edit still blocked
• Prompt: exit mismatch = strong FAIL; output mismatch = suspicious

Trust stack (still local-first)
1. HMAC integrity (1.1) — was it harness-written?
2. Independent re-exec (1.2) — does the command still match?
3. Residual: full FS access to attest.key; flaky tests can yield output_mismatch (surfaced, not hidden)

Tests
30/30 pass — verified-only counts, status snapshot, forged-only review bundle, match / exit_mismatch / output_mismatch, bundle embeds re-exec.

Docs updated: AGENTS.md, ROADMAP.md (Phase 1.2), README.md, reviewer prompt, example configs.