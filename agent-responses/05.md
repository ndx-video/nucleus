Phase 2.1 — Daily-use polish

Honesty layers are unchanged. Daily UX is tighter.

1. Status (/nucleus / /n)

Scannable layout:

Nucleus
───────
Phase   idle (ready for /spec)
Role    planner
…
Attest  0 verified
Isol.   — (default /review = new_session)

⚠ Blocked: …          # when relevant

Next
  · /spec — draft a Nucleus Spec …

Includes: phase, role, change, verified att count + latest id, last isolation mode, blocked reason, next actions.

2. Command feedback

Short, actionable errors (Cannot /implement in idle. Next: /spec then /spec approve.). Quieter success paths (one notify, less toast spam).

3. Review Bundle

• At a glance table  
• Clear §1–4 separators  
• Re-exec summary table with MATCH / EXIT MISMATCH / OUTPUT MISMATCH  
• Shorter isolation kickoff  

4. Defaults & examples

• Annotated nucleus.config.example.yaml (paths, setup, .nucleus/ layout)  
• Lean examples/nucleus.yaml + examples/README.md  

5. Packaging

• package.json files list for clean install/publish  
• Entry points unchanged (src/index.ts, skills, prompts)  

6. Hygiene

• No toast on every normal session_start (footer only)  
• Single isolation notify on /review  

Tests

39/39 pass, tsc clean.

Docs

README, AGENTS.md, ROADMAP (Phase 2.1 section) updated for user-visible behaviour.

Ready for a one-liner install + short docs page when you want to ship.